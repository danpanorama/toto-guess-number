class Point{
     point(element,arr){
          

for(let i = 0; i < arr.length; i++){
    if(arr[i].val == ""){
      let ans=  document.getElementById(arr[i].id);
      ans.value = element;
      let program = new Check;
      program.active(element);

      let colori = new Color;
      colori.active()

   let cheacingfor = new Config;
cheacingfor.check(arr[i].id)

let activTheScan = new Scan;
activTheScan.activ(arr[i].id)

return 

        
    }
}
 


     }
}